{
  "extends": ["react-app", "eslint-config-react-app"],
  "overrides": [
    {
      "files": ["src/**/*.ts?(x)"],
      "rules": {
        "no-multi-spaces": "warn",
        "no-console": "warn",
        "no-unused-vars": "warn",
        "no-trailing-spaces": "warn"
      }
    }
  ]
}